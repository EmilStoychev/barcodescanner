<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="pageLoaded">
  <StackLayout>
    <Button text="Scan" tap="{{ scan }}" />
    <Label text="Format" />
    <TextView text="{{ format }}" />
    <Label text="Content" />
    <TextView text="{{ content }}" />
  </StackLayout>
</Page>